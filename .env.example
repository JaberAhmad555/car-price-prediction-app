# Empty placeholders only. See README for shell configuration commands.
# Backend reads process environment, not implicit .env files.
DATABASE_URL=
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
NEXT_PUBLIC_API_URL=
# Future artifact selection must use an eligible versioned Bangladesh bundle.
VALUATION_MODEL_VERSION=
CORS_ORIGINS=
CARVALUE_MODEL_DIR=
