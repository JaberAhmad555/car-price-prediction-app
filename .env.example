# Empty placeholders only. Phase 1 tests do not use these variables.
# Copy to .env for future local configuration; never commit real values.
DATABASE_URL=
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
NEXT_PUBLIC_API_BASE_URL=
# Future artifact selection must use an eligible versioned Bangladesh bundle.
VALUATION_MODEL_VERSION=
